# =============================================================================
# xuni 量化交易系统 - 依赖锁定
# Python: 3.12.x（推荐，已在本机验证）
# 主框架: Freqtrade 最新稳定版
# 环境: CPU-only / Windows 本地优先
# =============================================================================

# --- 核心交易框架 ---
freqtrade>=2026.6,<2027.0

# --- Freqtrade 运行期补齐（2026.6 metrics 需要 scipy）---
scipy>=1.11.0,<2.0

# --- Hyperopt（Phase 5 参数寻优）---
optuna>=3.0.0,<5.0

# --- 数据处理（Freqtrade 已依赖，显式钉版本便于复现）---
pandas>=2.2.0,<3.0
numpy>=1.26.0,<3.0

# --- 配置与环境 ---
python-dotenv>=1.0.0,<2.0

# --- 测试 ---
pytest>=8.0.0,<9.0
pytest-cov>=5.0.0

# --- 工具 ---
tabulate>=0.9.0
rich>=13.0.0

# 说明:
# 1. 不安装 GPU / torch / tensorflow（当前阶段不做 ML）
# 2. 策略模板不依赖 TA-Lib，避免 Windows 编译问题
# 3. 安装命令见 README.md
