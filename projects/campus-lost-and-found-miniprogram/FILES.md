# 文件清单

当前 GitHub 目录已上传小程序入口文件，便于快速查看项目结构。

## 已上传文件

- `README.md`：项目说明
- `app.js`：小程序入口文件
- `app.json`：页面路由配置
- `project.config.example.json`：微信开发者工具配置示例

## 本地整理版本包含

- `pages/index/`：首页展示、分类筛选和信息卡片
- `components/navigation-bar/`：自定义导航栏组件
- `images/`：分类图片和首页背景资源
- `app.wxss`：全局样式

如果后续要重点展示这个项目，建议拆成独立仓库并上传完整目录。
