# 文件清单

当前 GitHub 目录已上传小程序入口文件，便于 HR 或面试官快速判断项目类型和结构。

## 已上传文件

- `README.md`：项目说明
- `app.js`：小程序入口逻辑
- `app.json`：小程序页面与 tabBar 配置
- `project.config.example.json`：微信开发者工具配置示例

## 本地整理版本包含

- `pages/`：借用确认、押金、帮助、订单、站点、个人中心等页面
- `assets/`：图标与页面图片资源
- `config/`：项目常量配置
- `mock/`：演示数据
- `utils/`：本地存储、设备与订单相关工具函数

如果后续要重点展示这个小程序，建议单独创建仓库并上传完整目录。
